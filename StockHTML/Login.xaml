<Window x:Class="StockHTML.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        Height="400"
        Width="350"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#1e1e2f">

    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="EMAIL" Foreground="White" Margin="5,5,0,0"/>
            <TextBox x:Name="EmailBox" />

            <TextBlock Text="PASSWORD" Foreground="White" Margin="5,10,0,0"/>
            <PasswordBox x:Name="PasswordBox" />

            <Button Content="SIGN IN" 
                    Click="ValidateLOGIN" />
        </StackPanel>
    </Grid>
</Window>
